@model IEnumerable<MODELS.CUSTOMER>

@{
    ViewBag.Title = "info_user";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>info_user</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
      
        <th>
            @Html.DisplayNameFor(model => model.NAME)
        </th>
     
        <th>
            @Html.DisplayNameFor(model => model.AMOUNT)
        </th>
      
        <th>
            @Html.DisplayNameFor(model => model.ACCNO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
    
        <td>
            @Html.DisplayFor(modelItem => item.NAME)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.AMOUNT)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.ACCNO)
        </td>
        <td>
            @Html.ActionLink("TRANSFER", "FUND_TRANSFER", new { accno = item.ACCNO })     
            @Html.ActionLink("Deposit", "Deposit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Withdraw", "Withdraw", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
